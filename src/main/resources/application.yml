server:
  port: 8080

logging:
  file:
    path: logs

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  output:
    ansi:
      enabled: always

  jackson:
    default-property-inclusion: non_null

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/soosoodb?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
    username: soosoo
    password: soosoo7942!
    driver-class-name: co.mysql.cj.jdbc.Driver